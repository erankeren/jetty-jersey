{
    "package": {
        "name": "jetty-jersey", // Bintray package name
        "repo": "mvn-repo", // Bintray repository name
        "subject": "erankeren", // Bintray subject (user or organization)
    },

    "version": {
        "name": "1.0.0",
    },

    "files":
        [
          {
            "includePattern": "target/(*.jar)",
            "excludePattern": "target/.*.zip$", 
            "uploadPattern": "/$1"
          }
        ],
    "publish": true
}
